// Archivo creado en TS para repasar los tipos de datos

// definimos constantes para exportar al Main
export const name: string = 'janania';
export const age: number = 40;
export const isValid: boolean = true;

// string multilinea
const templateString = `nombre= ${name}
edad= ${age}
menor a 50?= ${isValid}`

// mostramos por consola
console.log(templateString);

